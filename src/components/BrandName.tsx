export function BrandName({ className = "" }: { className?: string }) {
  return (
    <span className={className}>
      En<span className="gradient-text font-semibold">Ai</span>bleWorks
    </span>
  );
}
